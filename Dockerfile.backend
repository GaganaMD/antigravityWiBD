FROM python:3.11-slim

WORKDIR /app

COPY agent-pipeline/python-backend/requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY agent-pipeline/python-backend .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]